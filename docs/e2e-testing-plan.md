# План E2E тестирования FlickToPick

## Введение

Данный документ описывает сценарии сквозного (end-to-end) тестирования MVP системы «FlickToPick» — приложения для совместного выбора фильмов.

**Охват MVP**: 
- Создание и управление сессиями выбора фильмов
- Получение рекомендаций фильмов на основе жанров и рейтинга
- Обнаружение совпадений (матчей) когда все участники проголосовали "нравится"

**Предусловия для всех тестов**:
- Все сервисы системы развернуты и доступны
- API Gateway доступен по адресу `http://localhost:8080`
- Система инициализирована с тестовыми данными о фильмах

**Способ выполнения тестов**: REST API через HTTP-запросы

---

# ЧАСТЬ 1: СЦЕНАРИИ УПРАВЛЕНИЯ СЕССИЯМИ

## 1.1. Основная функциональность сессий

### Тест 1.1.1: Успешное создание новой сессии

**Цель**: проверить полный цикл создания сессии и получение кода приглашения.

**Предусловия**:
- Session Service доступен на http://localhost:8001
- Base данные инициализирована

**Шаги**:
1. Отправить HTTP-запрос:
   ```
   POST http://localhost:8001/sessions/create
   Headers:
     Content-Type: application/json
   Body:
   {
     "creator_id": "user-001"
   }
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- Тело ответа содержит:
  ```json
  {
    "session_id": 1,
    "session_code": "ABC123def",
    "creator_id": "user-001",
    "status": "active",
    "current_movie_id": null,
    "participants": ["user-001"],
    "created_at": "2025-12-12T10:00:00"
  }
  ```
- Сессия создана в БД с статусом "active"
- Код сессии состоит из 6 символов (буквы и цифры)
- Создатель сессии добавлен как первый участник

### Тест 1.1.2: Подключение пользователя к существующей сессии по коду

**Цель**: проверить корректное добавление нового пользователя в существующую сессию.

**Предусловия**:
- Сессия существует с кодом "ABC123"
- Сессия имеет статус "active"
- Пользователь "user-002" еще не является участником

**Шаги**:
1. Отправить HTTP-запрос:
   ```
   POST http://localhost:8001/sessions/join
   Headers:
     Content-Type: application/json
   Body:
   {
     "session_code": "ABC123",
     "user_id": "user-002"
   }
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- Тело ответа содержит:
  ```json
  {
    "session_id": 1,
    "session_code": "ABC123",
    "creator_id": "user-001",
    "status": "active",
    "current_movie_id": null,
    "participants": ["user-001", "user-002"],
    "created_at": "2025-12-12T10:00:00"
  }
  ```
- Пользователь добавлен в список participants
- Пользователь отмечен как active (is_active=true)

### Тест 1.1.3: Попытка присоединиться к несуществующей сессии

**Цель**: проверить обработку ошибки при попытке подключиться к несуществующей сессии.

**Предусловия**:
- Сессия с кодом "INVALID" не существует в системе

**Шаги**:
1. Отправить HTTP-запрос:
   ```
   POST http://localhost:8001/sessions/join
   Headers:
     Content-Type: application/json
   Body:
   {
     "session_code": "INVALID",
     "user_id": "user-003"
   }
   ```

**Ожидаемый результат**:
- HTTP статус: 404 Not Found
- Тело ответа содержит:
  ```json
  {
    "detail": "Session not found"
  }
  ```
- Пользователь не добавлен в базу данных
- Никакие данные не изменились

### Тест 1.1.4: Попытка присоединиться дважды одним пользователем

**Цель**: проверить обработку попытки присоединиться к сессии, когда пользователь уже в ней.

**Предусловия**:
- Сессия существует с кодом "SESSION1"
- Пользователь "user-004" уже является участником этой сессии

**Шаги**:
1. Отправить HTTP-запрос на подключение пользователя "user-004" к сессии "SESSION1"
   ```
   POST http://localhost:8001/sessions/join
   Headers:
     Content-Type: application/json
   Body:
   {
     "session_code": "SESSION1",
     "user_id": "user-004"
   }
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- Пользователь уже активен (is_active переходит с false на true если он был неактивен)
- participants список содержит "user-004"
- Запись не дублируется в БД

### Тест 1.1.5: Получение информации о сессии

**Цель**: проверить корректное получение полной информации о сессии.

**Предусловия**:
- Сессия существует с кодом "ABC123"
- В сессии 3 активных участника

**Шаги**:
1. Отправить HTTP-запрос:
   ```
   GET http://localhost:8001/sessions/ABC123
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- Тело ответа содержит полную информацию:
  ```json
  {
    "session_id": 1,
    "session_code": "ABC123",
    "creator_id": "user-001",
    "status": "active",
    "current_movie_id": null,
    "participants": ["user-001", "user-002", "user-003"],
    "created_at": "2025-12-12T10:00:00"
  }
  ```
- Возвращены только активные участники (is_active=true)

### Тест 1.1.6: Завершение сессии и обновление текущего фильма

**Цель**: проверить обновление статуса сессии и текущего фильма.

**Предусловия**:
- Сессия существует с кодом "ABC123"
- Статус сессии: "active"

**Шаги**:
1. Отправить HTTP-запрос на завершение сессии:
   ```
   POST http://localhost:8001/sessions/ABC123/complete
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- Тело ответа содержит:
  ```json
  {
    "status": "completed",
    "session_code": "ABC123"
  }
  ```
- Статус сессии в БД изменился на "completed"

### Тест 1.1.6b: Обновление текущего фильма

**Цель**: проверить обновление текущего фильма в сессии.

**Предусловия**:
- Сессия существует с кодом "ABC123"
- current_movie_id: null

**Шаги**:
1. Отправить HTTP-запрос:
   ```
   PUT http://localhost:8001/sessions/ABC123/movie
   Headers:
     Content-Type: application/json
   Body:
   {
     "current_movie_id": 5
   }
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- current_movie_id в ответе = 5

---

## 1.2. Обработка ошибок Session Service

### Тест 1.2.1: Попытка присоединиться к завершенной сессии

**Цель**: проверить, что нельзя подключиться к завершенной сессии.

**Предусловия**:
- Сессия существует с кодом "CLOSED123"
- Статус сессии: "completed"

**Шаги**:
1. Отправить HTTP-запрос на подключение к завершенной сессии:
   ```
   POST http://localhost:8001/sessions/join
   Headers:
     Content-Type: application/json
   Body:
   {
     "session_code": "CLOSED123",
     "user_id": "user-006"
   }
   ```

**Ожидаемый результат**:
- HTTP статус: 400 Bad Request
- Тело ответа содержит:
  ```json
  {
    "detail": "Session is completed"
  }
  ```
- Пользователь не добавлен в сессию

### Тест 1.2.2: Отключение пользователя от сессии

**Цель**: проверить корректное отключение пользователя.

**Предусловия**:
- Сессия существует с кодом "ABC123"
- user-002 является активным участником

**Шаги**:
1. Отправить HTTP-запрос:
   ```
   POST http://localhost:8001/sessions/ABC123/disconnect/user-002
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- Тело ответа:
  ```json
  {
    "status": "disconnected",
    "session_code": "ABC123",
    "user_id": "user-002"
  }
  ```
- is_active пользователя изменился на false
- Пользователь больше не в списке активных participants

---

# ЧАСТЬ 2: СЦЕНАРИИ ПОЛУЧЕНИЯ РЕКОМЕНДАЦИЙ

## 2.1. Основная функциональность рекомендаций

### Тест 2.1.1: Успешное получение списка фильмов для сессии

**Цель**: проверить получение списка доступных фильмов для новой сессии.

**Предусловия**:
- Сессия существует с кодом "ABC123"
- В базе данных существует минимум 5 доступных фильмов
- Сессия активна в Session Service

**Шаги**:
1. Отправить HTTP-запрос:
   ```
   GET http://localhost:8002/movies?session=ABC123
   Headers:
     Content-Type: application/json
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- Тело ответа содержит массив фильмов:
  ```json
  [
    {
      "id": 1,
      "title": "The Matrix",
      "genre": "sci-fi",
      "duration_minutes": 136,
      "rating": 8.7,
      "description": "A hacker discovers the true nature of his reality",
      "poster_url": "https://...",
      "is_available": true
    },
    {
      "id": 2,
      "title": "Inception",
      "genre": "sci-fi",
      "duration_minutes": 148,
      "rating": 8.8,
      "description": "...",
      "poster_url": "https://...",
      "is_available": true
    },
    ...
  ]
  ```
- Возвращены все доступные фильмы (is_available = true)
- Каждый фильм содержит обязательные поля (id, title, genre, duration_minutes, rating, description, poster_url, is_available)
- Фильмы отсортированы по рейтингу (убывание)

### Тест 2.1.2: Получение рекомендации для следующего фильма

**Цель**: проверить получение одного рекомендуемого фильма при знании текущего.

**Предусловия**:
- Сессия существует с кодом "ABC123"
- Текущий просматриваемый фильм имеет ID = 1
- В базе данных есть фильмы с ID > 1

**Шаги**:
1. Отправить запрос на получение следующей рекомендации:
   ```
   GET http://localhost:8002/recommendation/next?session=ABC123&current_movie=1
   Headers:
     Content-Type: application/json
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- Тело ответа содержит рекомендуемый фильм:
  ```json
  {
    "id": 2,
    "title": "Inception",
    "genre": "sci-fi",
    "duration_minutes": 148,
    "rating": 8.8,
    "description": "...",
    "poster_url": "https://...",
    "is_available": true
  }
  ```
- Возвращаемый фильм отличается от текущего (id != 1)
- Фильм имеет is_available = true

### Тест 2.1.3: Получение фильмов для сессии с несколькими участниками

**Цель**: проверить, что рекомендации работают для всех участников сессии.

**Предусловия**:
- Сессия существует с кодом "MULTI123" с 3 участниками
- В базе данных есть минимум 5 фильмов
- Сессия существует в Session Service

**Шаги**:
1. Запросить список фильмов:
   ```
   GET http://localhost:8002/movies?session=MULTI123
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- Возвращены фильмы для всех участников сессии
- Все фильмы доступны (is_available = true)
- Возвращено минимум 5 фильмов
- Каждый фильм имеет все обязательные поля

### Тест 2.1.4: Валидация сессии при запросе фильмов

**Цель**: проверить, что Recommendation Service валидирует сессию через Session Service.

**Предусловия**:
- Сессия существует с кодом "ABC123" в Session Service

**Шаги**:
1. Запросить фильмы:
   ```
   GET http://localhost:8002/movies?session=ABC123
   ```

**Ожидаемый результат**:
- HTTP статус: 200 OK
- Recommendation Service успешно валидировал сессию через Session Service
- Возвращены фильмы для данной сессии
- В логах виден вызов Session Service на валидацию

---

## 2.2. Обработка ошибок Recommendation Service

### Тест 2.2.1: Попытка получить фильмы для несуществующей сессии

**Цель**: проверить обработку ошибки при запросе фильмов для несуществующей сессии.

**Предусловия**:
- Сессия с кодом "INVALID000" не существует в Session Service

**Шаги**:
1. Отправить HTTP-запрос:
   ```
   GET http://localhost:8002/movies?session=INVALID000
   Headers:
     Content-Type: application/json
   ```

**Ожидаемый результат**:
- HTTP статус: 400 Bad Request или 404 Not Found
- Тело ответа содержит сообщение об ошибке (example):
  ```json
  {
    "detail": "Session validation failed"
  }
  ```
- Никакие фильмы не возвращены

### Тест 2.2.2: Попытка получить рекомендацию с некорректным ID фильма

**Цель**: проверить обработку ошибки при некорректном ID текущего фильма.

**Предусловия**:
- Сессия существует с кодом "ABC123"

**Шаги**:
1. Отправить запрос с несуществующим ID фильма:
   ```
   GET http://localhost:8002/recommendation/next?session=ABC123&current_movie=99999
   Headers:
     Content-Type: application/json
   ```

**Ожидаемый результат**:
- HTTP статус: 400 Bad Request
- Рекомендация не возвращена или возвращена дефолтная

---

# ЧАСТЬ 3: СЦЕНАРИИ ГОЛОСОВАНИЯ И МЭТЧИНГА

## 3.1. Основная функциональность Match Service

### Тест 3.1.1: Успешное обнаружение совпадения (матча)

**Цель**: проверить полный цикл обнаружения совпадения когда все участники проголосовали "хотим смотреть".

**Предусловия**:
- Сессия существует с session_id = 1 и 3 активными участниками
- Все участники могут голосовать
- Текущий фильм: movie_id = 1

**Шаги**:
1. Участник user-001 голосует "want_now" для movie_id=1:
   ```
   POST http://localhost:8000/swipe
   Headers:
     Content-Type: application/json
   Body:
   {
     "session_id": 1,
     "user_id": "user-001",
     "movie_id": 1,
     "swipe_value": "want_now",
     "participants": ["user-001", "user-002", "user-003"]
   }
   ```
2. Участник user-002 голосует "want_now" для movie_id=1 с теми же participants
3. Участник user-003 голосует "want_now" для movie_id=1 с теми же participants

**Ожидаемый результат**:
- Первый голос (user-001): HTTP 200, тело:
  ```json
  {
    "status": "vote_recorded",
    "session_id": 1,
    "movie_id": 1,
    "votes_count": 1,
    "required_votes": 3
  }
  ```
- Второй голос (user-002): HTTP 200, тело:
  ```json
  {
    "status": "vote_recorded",
    "session_id": 1,
    "movie_id": 1,
    "votes_count": 2,
    "required_votes": 3
  }
  ```
- Третий голос (user-003): HTTP 200, тело содержит:
  ```json
  {
    "status": "match_found",
    "session_id": 1,
    "movie_id": 1,
    "matched_movie": {
      "id": 1,
      "title": "..."
    }
  }
  ```
- Матч обнаружен
- Голоса сохранены в Redis

### Тест 3.1.2: Отсутствие совпадения при пропуске одного участника

**Цель**: проверить, что матча нет, если хотя бы один участник проголосовал "skip".

**Предусловия**:
- Сессия существует с session_id = 2 и 3 участниками
- Текущий фильм: movie_id = 2

**Шаги**:
1. Участник user-001 голосует "want_now" для movie_id=2:
   ```
   POST http://localhost:8000/swipe
   Body: {"session_id": 2, "user_id": "user-001", "movie_id": 2, "swipe_value": "want_now", "participants": ["user-001", "user-002", "user-003"]}
   ```
2. Участник user-002 голосует "want_now" для movie_id=2
3. Участник user-003 голосует "skip" для movie_id=2

**Ожидаемый результат**:
- Первый голос: HTTP 200, status: "vote_recorded"
- Второй голос: HTTP 200, status: "vote_recorded"
- Третий голос: HTTP 200, содержит:
  ```json
  {
    "status": "next_movie",
    "session_id": 2,
    "movie_id": 2,
    "message": "Not all participants wanted this movie"
  }
  ```
- Матч НЕ создается
- Рекомендуется перейти к следующему фильму

---

## 3.2. Обработка ошибок Match Service

### Тест 3.2.1: Попытка голосовать для несуществующей сессии

**Цель**: проверить обработку ошибки при голосовании в несуществующей сессии.

**Предусловия**:
- Сессия с session_id = 99999 не существует

**Шаги**:
1. Отправить HTTP-запрос:
   ```
   POST http://localhost:8000/swipe
   Headers:
     Content-Type: application/json
   Body:
   {
     "session_id": 99999,
     "user_id": "user-001",
     "movie_id": 1,
     "swipe_value": "want_now",
     "participants": ["user-001"]
   }
   ```

**Ожидаемый результат**:
- HTTP статус: 400 Bad Request или 404 Not Found
- Тело ответа содержит сообщение об ошибке (примерный формат):
  ```json
  {
    "detail": "Session not found or invalid"
  }
  ```

### Тест 3.2.2: Попытка голосовать дважды за один фильм

**Цель**: проверить поведение при повторном голосовании за тот же фильм.

**Предусловия**:
- Сессия существует с session_id = 4
- Пользователь user-001 уже проголосовал "want_now" для movie_id=4
- Голос сохранен в Redis

**Шаги**:
1. Попытка проголосовать еще раз для того же фильма с другим значением:
   ```
   POST http://localhost:8000/swipe
   Headers:
     Content-Type: application/json
   Body:
   {
     "session_id": 4,
     "user_id": "user-001",
     "movie_id": 4,
     "swipe_value": "skip",
     "participants": ["user-001", "user-002"]
   }
   ```

**Ожидаемый результат**:
- HTTP статус: 400 Bad Request или 409 Conflict
- Тело ответа содержит сообщение об ошибке:
  ```json
  {
    "detail": "User has already voted for this movie"
  }
  ```

### Тест 3.2.3: Голос пользователя, не являющегося участником сессии

**Цель**: проверить, что голосовать могут только участники из списка participants.

**Предусловия**:
- Сессия существует с session_id = 5 с участниками: user-001, user-002
- Пользователь user-999 НЕ является участником

**Шаги**:
1. Попытка проголосовать от пользователя user-999:
   ```
   POST http://localhost:8000/swipe
   Headers:
     Content-Type: application/json
   Body:
   {
     "session_id": 5,
     "user_id": "user-999",
     "movie_id": 5,
     "swipe_value": "want_now",
     "participants": ["user-001", "user-002"]
   }
   ```

**Ожидаемый результат**:
- HTTP статус: 400 Bad Request или 403 Forbidden
- Тело ответа содержит сообщение об ошибке:
  ```json
  {
    "detail": "User is not a participant of this session"
  }
  ```

---

# ЧАСТЬ 4: ПОЛНЫЕ ПОЛЬЗОВАТЕЛЬСКИЕ СЦЕНАРИИ (E2E)

## 4.1. Полный жизненный цикл сессии выбора фильма

### Тест 4.1.1: Полный сценарий от создания сессии до матча

**Цель**: проверить полный цикл работы системы: создание сессии, получение фильмов, голосование, матч.

**Предусловия**:
- Все сервисы развернуты (Session Service:8001, Recommendation Service:8002, Match Service:8000)
- В базе данных MovieDB минимум 5 фильмов с рейтингом выше 7.0

**Шаги**:
1. Создать новую сессию:
   ```
   POST http://localhost:8001/sessions/create
   Body: {"creator_id": "user-001"}
   → Получить session_id (int), session_code (6 символов)
   ```
2. Добавить 2 участников к сессии:
   ```
   POST http://localhost:8001/sessions/join
   Body: {"session_code": "<code>", "user_id": "user-002"}
   
   POST http://localhost:8001/sessions/join
   Body: {"session_code": "<code>", "user_id": "user-003"}
   ```
3. Получить список фильмов для сессии:
   ```
   GET http://localhost:8002/movies?session=<session_code>
   → Получить массив Movie объектов
   ```
4. Все участники голосуют "want_now" за первый фильм (movie_id=1):
   ```
   POST http://localhost:8000/swipe
   Body: {"session_id": <session_id>, "user_id": "user-001", "movie_id": 1, "swipe_value": "want_now", "participants": ["user-001", "user-002", "user-003"]}
   
   POST http://localhost:8000/swipe (user-002)
   POST http://localhost:8000/swipe (user-003)
   ```
5. Проверить результат матча после третьего голоса
6. Завершить сессию:
   ```
   POST http://localhost:8001/sessions/<session_code>/complete
   ```

**Ожидаемый результат**:
- Сессия создана, код получен (200 OK)
- Оба пользователя добавлены (200 OK, 200 OK)
- Получены фильмы из базы данных (200 OK)
- Первые два голоса: status="vote_recorded"
- Третий голос: status="match_found" с данными о фильме
- Сессия завершена, status="completed" (200 OK)

---

## 4.2. Обработка граничных случаев

### Тест 4.2.1: Сессия с одним участником

**Цель**: проверить, может ли одиночный пользователь использовать систему.

**Предусловия**:
- Одиночная сессия создана с session_id = 19, session_code = "SOLO19"
- Только создатель user-001 (participants = ["user-001"])

**Шаги**:
1. Получить фильмы: 
   ```
   GET http://localhost:8002/movies?session=SOLO19
   ```
2. Проголосовать "want_now" за первый фильм: 
   ```
   POST http://localhost:8000/swipe
   Body: {"session_id": 19, "user_id": "user-001", "movie_id": 1, "swipe_value": "want_now", "participants": ["user-001"]}
   ```
3. Проверить результат

**Ожидаемый результат**:
- Фильмы получены (200 OK)
- Один голос "want_now" от единственного участника считается матчем (status="match_found")
- Match Service определяет, что все участники (1 из 1) проголосовали
- Система позволяет одиночное использование

### Тест 4.2.2: Максимальное количество участников

**Цель**: проверить, работает ли система при большом количестве участников.

**Предусловия**:
- Сессия существует с session_id = 20, session_code = "BIG20"
- В базе данных есть фильмы

**Шаги**:
1. Добавить 10 участников в сессию через POST /sessions/join:
   - user-001 (создатель) + user-002 до user-010
2. Все 10 участников голосуют "want_now" за movie_id=1 через POST /swipe:
   - Отправить 10 запросов с participants=["user-001",...,"user-010"]

**Ожидаемый результат**:
- Все 10 участников успешно добавлены через Session Service (200 OK × 10)
- Первые 9 голосов: status="vote_recorded"
- 10-й голос: status="match_found"

---

# ЧАСТЬ 5: ПРОВЕРКА ТРЕБОВАНИЙ К ПРОИЗВОДИТЕЛЬНОСТИ

## Тест 5.1: Ответ на действия пользователя в течение 100 мс

**Цель**: проверить, что операции выполняются быстро (требование: менее 100 мс).

**Предусловия**:
- Сессия существует с session_id = 100, 3 участника
- Фильмы загружены в базу данных

**Шаги**:
1. Засечь текущее время (start_time)
2. Отправить голос через Match Service:
   ```
   POST http://localhost:8000/swipe
   Body: {"session_id": 100, "user_id": "user-001", "movie_id": 1, "swipe_value": "want_now", "participants": ["user-001", "user-002", "user-003"]}
   ```
3. Получить ответ (end_time)
4. Вычислить: response_time = end_time - start_time

**Ожидаемый результат**:
- Время ответа ≤ 100 мс (целевое значение ≤ 50 мс)
- HTTP статус 200 OK
- Response содержит status="vote_recorded" или "match_found"
